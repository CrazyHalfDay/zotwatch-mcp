# --- QQ 机器人凭据(必填）------------------------------------------------------
# 在 QQ 开放平台 https://q.qq.com 创建机器人后获取。
QQ_BOT_APPID=你的_appid
QQ_BOT_SECRET=你的_appsecret

# --- ZotWatch 项目路径 ---------------------------------------------------------
# 指向包含 config/config.yaml 和 data/ 的 AIZotWatch 目录。
# 本机直接跑时填这里;用 Docker 时由 docker-compose.yml 设成 /zotwatch。
ZOTWATCH_DIR=/path/to/AIZotWatch

# --- 可选 ----------------------------------------------------------------------
# 只允许这些 QQ openid 使用(逗号分隔)。留空=不限制。
# 注意:QQ 的 openid 按机器人加密,群里(member_openid)和私聊(user_openid)不同。
# QQ_BOT_ALLOWED_USERS=

# 每个列表显示几篇(可被 收藏/总结 用序号引用)。
# ZOTWATCH_BOT_LIST_LIMIT=8

# --- ZotWatch 自己的凭据(通常不必在这里重复)---------------------------------
# 从 ZOTWATCH_DIR/.env 读取,列在这里仅作参考:
# ZOTERO_API_KEY=...
# ZOTERO_USER_ID=...
# VOYAGE_API_KEY=...           # 或 DASHSCOPE_API_KEY,按 embedding.provider
# DEEPSEEK_API_KEY=...         # 或 MOONSHOT_API_KEY / OPENROUTER_API_KEY
